import { css } from "@emotion/react";
import { theme } from "./theme";

export const border1 = css`
    border-width: ${theme.borders.border1Width};
    border-style: ${theme.borders.border1Style};
    border-color: ${theme.borders.border1Color};
`;
